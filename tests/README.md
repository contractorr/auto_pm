# Tests

Tests enforce the spec-driven skeleton:

- contracts parse and round-trip
- config examples stay valid
- required specs remain present
- harness fixtures keep matching current models
- dry-run pipeline fixtures keep deterministic synthesis behavior stable
